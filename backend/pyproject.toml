[tool.ruff]
line-length = 88
target-version = "py312"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
# B008 is the idiomatic FastAPI Depends() default-argument pattern.
ignore = ["B008"]

[tool.pytest.ini_options]
testpaths = ["tests"]
